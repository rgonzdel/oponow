# Para desarrollo con Expo Go en un teléfono físico, "localhost" apunta al
# propio teléfono, no a tu ordenador — usa la IP de tu ordenador en la
# misma red WiFi (ej. 192.168.1.23), no "localhost".
# Contra producción, usa la API real:
EXPO_PUBLIC_API_URL=https://api.oponow.com
